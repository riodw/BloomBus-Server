/*
 * IConstants.ts
 * 
 * Defines the structure and types of the object at the 'constants' ref in Firebase.
 */

interface IConstants {
  reapShuttleThresholdMilliseconds: number,
  stopProximityThresholdMeters: number
}
export default IConstants;